// Contains a function ArrMax.arrMax that finds and returns the largest element in the array a of length b.
// a & b are both supplied as arguments


// Put your code here.
function ArrMax.arrMax 2
push argument 1
pop static 3 // storing the length as a new variable
push argument 0
pop static 4 //storing the max as a static for fun

label LOOP
    //checking that we are in range
    push static 3
    push constant 0
    eq
    if-goto END 

    // we are in range woo



label END
    goto END


